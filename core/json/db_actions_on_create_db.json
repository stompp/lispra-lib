[
    {
        "action": "executeStatements",
        "statements":
                ["CREATE TABLE IF NOT EXISTS lispra_users(user_id INT NOT NULL AUTO_INCREMENT,email VARCHAR(80) NOT NULL,password CHAR(50) NOT NULL,display_name VARCHAR(50) NOT NULL,PRIMARY KEY (user_id),UNIQUE INDEX (email));",
                "CREATE TABLE IF NOT EXISTS lispra_wp_users(ID INT NOT NULL,PRIMARY KEY (ID),UNIQUE INDEX (ID)"
                ]
    }
]


